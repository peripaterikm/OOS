# Mini-Epic - 7.1 Kill Archive Feedback

## Summary

Roadmap v2.4 item `7.1` adds deterministic Kill Archive feedback so candidate signals that resemble already-killed opportunities are downgraded for founder review instead of repeatedly elevated.

## Scope

- Added `src/oos/kill_archive_feedback.py`.
- Added `kill_pattern_flag` and `kill_pattern_penalty` scoring support.
- Integrated Kill Archive lookup into weekly discovery runs.
- Wrote `kill_archive_warnings.json` artifacts for founder package quality sections.
- Preserved similar killed opportunity, kill reason, and evidence linkage.

## Out Of Scope

- No semantic embeddings or vector search.
- No live LLM/API calls.
- No live internet/API calls.
- No auto-kill or portfolio state transition.
- No new dependencies.

## Validation

- Focused kill archive feedback tests passed.
- Full unittest discovery passed.
- `scripts/oos-validate.ps1` passed.
- `git diff --check` passed.

## Remaining Gaps

Roadmap v2.4 final closure remains blocked by the final `8.2` completion checkpoint rerun.
