# Mini-Epic 1.1 - Weekly Run Artifact Contract

## Goal

Define the exact set of artifacts that one v2.6 weekly cycle run must produce. This is the contract — every consumer (founder inbox, status command, run reports, import, replays) reads from these well-known paths.

## Scope

- Added `src/oos/weekly_run_manifest.py`.
- Defined `WeeklyRunManifest` dataclass with `run_id`, `created_at`, `schema_version`, `artifact_paths`, `artifact_schema_versions`, `empty_states`, `input_file`, `input_signal_count`, and advisory/no-live safety flags.
- Defined 14 canonical artifact paths under `artifacts/weekly_runs/{run_id}/`:
  - `manifest.json` — `WeeklyRunManifest`
  - `evidence_packs.json` — EvidencePack[]
  - `opportunity_candidates.json` — OpportunityCandidate[]
  - `quality_gate_decisions.json` — QualityGateDecision[]
  - `founder_decisions_v2.json` — FounderDecisionV2[]
  - `founder_feedback_mappings.json` — FounderFeedbackMapping[]
  - `founder_preference_profile.json` — FounderPreferenceProfile
  - `weekly_opportunity_review.json` — WeeklyOpportunityReviewPackage
  - `next_best_actions.json` — FounderAction[]
  - `parking_lot_records.json` — ParkingLotRecord[]
  - `run_report.json` — WeeklyRunReport
  - `founder_inbox_v2_index.json` — machine-readable inbox index
  - `founder_inbox_v2.md` — human-readable Markdown inbox
  - `run_report.md` — human-readable Markdown run report
- Implemented deterministic `run_id` generation: `weekly_run_{YYYY_MM_DD}_{content_hash_short}` where `content_hash = sha256(input_file_content + run_date)[:12]`.
- Implemented `write_weekly_run_manifest()` — validates and writes `manifest.json` to a run directory.
- Implemented `read_weekly_run_manifest()` — reads, parses, validates, and path-traversal-guards a `manifest.json`.
- Implemented `make_default_manifest()` — creates a fully-populated manifest with all 14 artifact paths, schema versions, and empty-state flags.
- Validation rejects: missing required fields, unknown schema versions, unknown artifact keys, path traversal (..), absolute paths, missing mandatory `manifest` entry, and false advisory/no-live flags.
- Empty-state run produces a valid manifest with all `empty_states` flags set to `true`.
- JSON roundtrip is stable: same `to_dict()` → `json.dumps()` → `json.loads()` → `from_dict()` produces equal manifest.
- Deterministic key ordering in `to_dict()` via canonical key list.

## Out Of Scope

- No full weekly cycle builder (item 2.1).
- No CLI integration (item 3.1).
- No founder inbox v2 (item 4.1).
- No founder decision import (item 5.1).
- No run reports / dashboard index (item 7.1).
- No real artifacts written under `artifacts/`.
- No live LLM/API calls.
- No live internet/API calls.

## Acceptance Criteria

- [x] `WeeklyRunManifest` model exists with `run_id`, `created_at`, `schema_version`, `artifact_paths`, `artifact_schema_versions`, `empty_states`, `input_file`, `input_signal_count`, `advisory_only`, `no_live_api`, `no_live_llm`.
- [x] `write_weekly_run_manifest()` writes `manifest.json` to a run directory and returns the manifest.
- [x] `read_weekly_run_manifest()` reads and validates a `manifest.json`.
- [x] Validation rejects missing required fields, unknown schema versions, and path traversal outside the run directory.
- [x] Empty-state run produces a valid manifest with all `empty_states` flags set to `true`.
- [x] A fixture test round-trips a full manifest with all 14 artifact paths.
- [x] `run_id` is deterministic: `weekly_run_{ISO_date}_{content_hash_short}`.
- [x] Full unittest discovery passes; focused tests pass (39/39 OK).

## Validation

- `powershell -NoProfile -Command "$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest tests.test_weekly_run_manifest -v"`
- `powershell -NoProfile -Command "$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest discover -s tests -p 'test_*.py' -v"`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\oos-validate.ps1`
- `git diff --check`

## Next Step

Roadmap v2.6 item `2.1` — Unified weekly cycle builder.
