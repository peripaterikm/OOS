# 1.5 Mojibake Cleanup Regression Test

## Goal

Prevent mojibake and encoding garbage from reaching founder-facing summaries, candidate signals, and founder package outputs.

## Scope

- Extend deterministic text cleanup for common UTF-8/Windows-1252 mojibake fragments.
- Cover first-run examples such as `todayвЂ™s` and `вЂў Small Businesses`.
- Apply cleanup through the existing `clean_evidence` path before candidate extraction and founder package rendering.
- Preserve `evidence_id` and `source_url` traceability.

## Implementation

- Extended `repair_mojibake` in `src/oos/evidence_classifier.py`.
- Added `mojibake_repaired` cleaning notes when raw evidence text changes.
- Added regression coverage in `tests/test_mojibake_cleanup.py`.
- Updated the existing cleaner/classifier test expectation for dash repair.

## Acceptance Criteria

- [x] `вЂ™` does not appear in cleaned/founder-facing summaries.
- [x] `вЂў` does not appear in cleaned/founder-facing summaries.
- [x] Cleanup does not corrupt valid UTF-8 punctuation.
- [x] Existing cleaner behavior is preserved.
- [x] Full validation passes.

## Out Of Scope

- No collector changes.
- No source artifact rewriting.
- No live collection.
- No live internet/API or LLM/API calls.
- No external dependencies.
