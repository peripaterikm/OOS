# Mini-Epic - 6.2 Cluster Synthesis LLM Contract

## Summary

Roadmap v2.4 item `6.2` defines a future-only cluster synthesis contract and deterministic local stub. The implementation summarizes 5-10 related signals as one cluster-level pattern without invoking any LLM provider.

## Scope

- Added `ClusterSynthesis` model.
- Added `src/oos/cluster_synthesis.py`.
- Added deterministic cluster synthesis stub.
- Added prompt contract and request builder for future use.
- Preserved evidence IDs and signal IDs.
- Verified `cluster_synthesis` budget role is available in local-preview policy.

## Out Of Scope

- No live LLM/API calls.
- No provider execution.
- No semantic clustering or embeddings.
- No pipeline redesign.
- No final Roadmap v2.4 closure.

## Validation

- Focused cluster synthesis contract tests passed.
- Full unittest discovery was blocked by Windows temp-directory ACL cleanup errors after `Ran 670 tests`.
- `scripts/oos-validate.ps1` was blocked by the same full-discovery temp cleanup behavior.
- `git diff --check` passed.

## Remaining Gaps

Roadmap v2.4 final closure remains blocked by full validation for `6.2`, `7.1`, and the final `8.2` checkpoint.
