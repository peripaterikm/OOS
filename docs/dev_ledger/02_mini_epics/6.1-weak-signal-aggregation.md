# Mini-Epic - 6.1 Weak Signal Aggregation Protocol

## Summary

Roadmap v2.4 item `6.1` adds deterministic weak signal aggregation so clusters of low-confidence signals from multiple sources can be elevated into founder-reviewable weak pattern candidates.

## Scope

- Added `WeakPatternCandidate` model.
- Added deterministic weak signal aggregation rules in `src/oos/weak_signal_aggregation.py`.
- Applied the exact roadmap threshold rule:
  - `signal_count >= 5`
  - `avg_confidence >= 0.30`
  - `source_diversity >= 2`
  - `max_confidence < 0.60`
- Preserved signal IDs, evidence IDs, sources, confidence values, and cluster key traceability.
- Integrated discovery runs by writing `weak_pattern_candidates.json`.
- Reused the existing founder package weak pattern section.

## Out Of Scope

- No semantic clustering or embeddings.
- No LLM behavior.
- No live collection changes.
- No scoring redesign.
- No final Roadmap v2.4 closure.

## Validation

- Focused weak aggregation tests passed.
- Full unittest discovery passed.
- `scripts/oos-validate.ps1` passed.
- `git diff --check` passed.

## Remaining Gaps

Roadmap v2.4 final closure remains blocked by `6.2`, `7.1`, and the final `8.2` checkpoint.
