# 3.1 Deterministic Opportunity Sketch Baseline Run Report

## Summary

- Branch: `feat/v2-5-block-3-opportunity-formation`
- Roadmap item: `3.1 Deterministic opportunity sketch baseline`
- Date: 2026-05-06
- Result: implemented and validated locally

## Implementation

- Added `src/oos/opportunity_sketch.py`.
- Added `tests/test_opportunity_sketch_baseline.py`.
- Updated Roadmap v2.5 and Dev Ledger state for item 3.1 completion.

## Behavior

- Builds `OpportunityCandidate` records from `EvidencePack` inputs only.
- Preserves evidence pack, evidence, source signal, and source URL traceability.
- Generates conservative problem statements for explicit cash-collection and month-end reporting evidence.
- Keeps buyer/product/price/why-now fields as `unknown` or unsupported when evidence does not support them.
- Lowers confidence for insufficient evidence and vendor/generic/source-quality risks.

## Validation

- `.\.venv\Scripts\python.exe -m unittest tests.test_opportunity_sketch_baseline -v`
- `.\.venv\Scripts\python.exe -m unittest tests.test_evidence_pack_builder -v`
- `.\.venv\Scripts\python.exe -m unittest tests.test_evidence_pack_contract -v`
- `.\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\oos-validate.ps1`
- `git diff --check`
- `git status --short --untracked-files=no`

## Safety

- No live collection run performed.
- No live internet/API calls performed.
- No live LLM/API calls performed.
- Unit tests remained fixture-only.
- No push, PR, merge, tag, or release performed.

## Roadmap State

- Current item updated to `3.2 LLM opportunity synthesis contract`.
- Completed updated to `10 / 24`.
- Remaining updated to `14 / 24`.
