# Run Report - 6.2 Cluster Synthesis LLM Contract

## Summary

Roadmap v2.4 item `6.2` added a cluster-level synthesis model, deterministic stub, and future-only prompt contract. The contract requires multiple related signals as context and explicitly rejects isolated single-signal synthesis as sufficient.

## Roadmap Item

- Roadmap: `docs/roadmaps/OOS_roadmap_v2_4_signal_quality_and_ai_layers_checklist.md`
- Item: `6.2 Cluster synthesis LLM contract`
- Branch: `feat/v2-4-final-validation-and-gap-closure`

## Commands Run

```powershell
git branch --show-current
git log --oneline -12
git status --short --untracked-files=no
Select-String -Path docs\roadmaps\OOS_roadmap_v2_4_signal_quality_and_ai_layers_checklist.md -Pattern "6.2" -Context 5,45
$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest tests.test_cluster_synthesis_contract -v
$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\oos-validate.ps1
git diff --check
git status --short --untracked-files=no
```

## Results Summary

| Check | Result | Evidence |
| --- | --- | --- |
| Focused cluster synthesis tests | pass | `tests.test_cluster_synthesis_contract` passed. |
| ClusterSynthesis model | pass | Model validates and roundtrips through model serialization. |
| Deterministic stub | pass | Repeated synthesis over the same cluster returns stable output. |
| Evidence preservation | pass | Strongest evidence IDs and evidence citations are selected deterministically. |
| Prompt contract | pass | Prompt includes cluster context, multiple signals, evidence citation rules, and isolated-signal prohibition. |
| Budget role | pass | `cluster_synthesis` is allowed by local-preview budget policy and remains fail-closed by default policy. |
| Full unittest discovery | blocked | Non-escalated discovery reached `Ran 670 tests` but failed with Windows temp-directory ACL cleanup errors; escalated rerun was unavailable in this session. |
| `scripts/oos-validate.ps1` | blocked | Standard validation hit the same Windows temp-directory ACL cleanup behavior because it runs full unittest discovery. |
| `git diff --check` | pass | No whitespace errors. |

## Blocked Commands

- Full unittest discovery and `scripts/oos-validate.ps1` were blocked by Windows temp-directory ACL cleanup errors in this session.
- Escalated rerun was requested for full unittest discovery, but the approval system rejected it because the usage limit was reached.

## Known Warnings

- The final Roadmap v2.4 closure remains blocked by `7.1` and final `8.2` completion.
- Full validation still requires a non-sandboxed rerun on this Windows environment because sandboxed temp-directory cleanup can hit ACL errors.

## Validation Evidence

- Focused tests: passed.
- Full tests: blocked by Windows temp-directory ACL cleanup errors after `Ran 670 tests`.
- Standard validation script: blocked by the same Windows temp-directory ACL cleanup behavior.
- Runtime artifacts created by tests/validation were not committed.

## Safety Evidence

- Dependencies added: `0`.
- Live internet/API calls: `0`.
- Live LLM/API calls: `0`.
- Provider execution calls added: `0`.
- Unit-test live network calls: `0`.
- Push performed: `no`.
- PR created: `no`.
- Merge performed: `no`.
- Tag created: `no`.
- Release created: `no`.

## Next Step

Continue gap closure with Roadmap v2.4 item `7.1 Kill Archive feedback into scoring`.
