# Run Report: 4.1 — Windows CLI Output Hardening

**Run ID**: `4.1-windows-cli-output-hardening`
**Date**: 2026-05-10 (backfilled pre-merge)
**Status**: PASS
**Mini Epic**: `docs/dev_ledger/02_mini_epics/4.1-windows-cli-output-hardening.md`
**Implementation Commit**: `e36a470`

## Scope

Replace fragile Unicode symbols in CLI output with ASCII-safe alternatives for CP1251/CP1252 Windows terminals. No business logic or correction semantics changed.

## Commands

```powershell
# Full test suite at implementation time (item 4.1 report)
$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -m pytest tests/ -q

# Controlled smoke
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run-controlled-smoke.ps1

# Git check
git diff --check
```

## Results

### 1. Unit Tests

- **Result**: **PASS**
- **Test count**: 1643 (implementation-time count from item 4.1 report)
- **Failures**: 0
- **Errors**: 0

### 2. Controlled Smoke Test

- **Result**: **PASS**
- **Steps**: All smoke steps pass with ASCII-safe output.
- **weekly-cycle-status-v2**: exit 0 confirmed.

### 3. ASCII-Only Content Check

- **Method**: Audit all terminal-facing output for Unicode codepoints outside ASCII range.
- **Result**: **PASS** — no non-ASCII codepoints in default output.

### 4. Git Diff Check

- **Result**: **PASS** — `git diff --check` clean.

### 5. What Was Changed

| File | Change | Rationale |
|------|--------|-----------|
| `src/oos/cli.py` | Unicode symbols → ASCII equivalents | All CLI subcommand output CP1251-safe |
| `src/oos/weekly_cycle_status.py` | Unicode symbols → ASCII equivalents, CP1251 fix | Status Markdown renderer CP1251-safe |
| `src/oos/weekly_run_reports.py` | Unicode symbols → ASCII equivalents | Report/dashboard renderers CP1251-safe |
| `src/oos/founder_inbox_v2.py` | Unicode symbols → ASCII equivalents (if any) | Inbox renderer CP1251-safe |
| `docs/runbooks/controlled_weekly_run_smoke_test.md` | Document ASCII-safe default, note `--utf8` deferred | Runbook reflects current state |
| `tests/` | ASCII-safe output assertions | Verify no mojibake-prone characters |

### 6. Symbol Replacement Map

| Original | Replacement | Context |
|----------|------------|---------|
| `✅` | `OK` | Pass/success markers |
| `❌` | `FAIL` | Fail/error markers |
| `⚠` | `WARN` | Warning markers |
| `─` (box-drawing) | `--` | Section separators |
| `◉` | `->` | Item bullets |

### 7. Deferred: `--utf8` Flag

The `--utf8` opt-in flag was **not implemented** in v2.8. Deferred to v2.9+.

## Notes

- No business logic changed. All decision values, counts, IDs, and semantic content are identical to pre-hardening output.
- No correction semantics changed. The replace/amend pipeline is unaffected.
- The ASCII-safe output is the only output mode in v2.8. No flag is needed to get safe output — it is the default.

## Follow-Ups

- v2.9+: Implement `--utf8` opt-in flag for terminals known to support UTF-8.
- v2.9+: Consider re-audit after any new CLI output modules are added.

## Self-Audit

| Question | Answer |
|----------|--------|
| Did this avoid feature changes? | Yes — cosmetic only |
| Did this avoid source/test/script changes beyond scope? | Yes |
| Did this avoid live APIs/LLMs? | Yes — no network, no LLM calls |
| Did this avoid push/PR/merge/tag? | Yes — local commit only |
| Were docs backfilled pre-merge? | Yes — mini-epic and run report created after v2.8 final validation |
