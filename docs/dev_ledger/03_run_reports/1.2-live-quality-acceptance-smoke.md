# Run Report - Roadmap v2.4 Item 1.2 Live Quality Acceptance Smoke

## Summary

- Branch: `feat/1-2-live-quality-acceptance-smoke`
- Roadmap item: v2.4 `1.2` - Live quality acceptance smoke.
- Scope: artifact-only live quality smoke checker, CLI report writer, tests, Dev Ledger, and roadmap update.
- Commit: pending (`Add live quality acceptance smoke`).

## Manual Smoke Evidence Used

Prior manual live smoke runs were treated as evidence inputs for the acceptance policy:

- `live_hn_002` succeeded and produced more finance-related HN signals after hardening.
- `live_github_003` succeeded and showed GitHub noise appears after quality gates.
- `live_mix_003` succeeded and exercised mixed-source live collection.
- `live_rss_002` succeeded as a controlled RSS skip with `rss_feed_url_missing`.

## Observed Outcomes

- HN results improved and included bookkeeping, invoice follow-up, accounting software, and small-business finance signals.
- GitHub install/tutorial content was downgraded by the prior hardening work.
- RSS no longer fails with `unknown url type`; missing feed URL is represented as a controlled skip.
- Founder package top signals are deduplicated by source URL.
- Known limitation remains: generic finance/consulting copy can still rank too high until semantic/LLM layers are added.

## Changes Made

- Added `src/oos/live_quality_smoke.py` for deterministic artifact-only smoke validation.
- Added CLI command `validate-live-quality-smoke`.
- Added focused fixture/temp artifact tests in `tests/test_live_quality_smoke.py`.
- Updated Roadmap v2.4 state to current item `2.1`, completed `2 / 17`, remaining `15 / 17`.
- Added this run report and mini-epic.

## Validation Evidence

| Command | Result |
| --- | --- |
| `$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -m unittest tests.test_live_quality_smoke -v` | Passed - 11 tests OK |
| `$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -m unittest tests.test_discovery_weekly_cli tests.test_founder_discovery_package_lite tests.test_live_collection_mode tests.test_candidate_signal_extractor tests.test_evidence_cleaner_classifier -v` | Passed - 100 tests OK |
| `$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v` | Passed - 508 tests OK after running outside the sandbox to avoid known Windows Temp ACL failures |
| `.\scripts\oos-validate.ps1` | Passed after running with PowerShell execution policy bypass outside the sandbox to avoid known Windows Temp ACL failures |
| `.\scripts\verify.ps1` | Passed after running with PowerShell execution policy bypass outside the sandbox to avoid known Windows Temp ACL failures |
| `git diff --check` | Passed |

## Known Local Warnings

Known ACL warnings may appear for `.test-tmp`, `.tmp_tests`, `.tmp_unittest`, `tmpcvst80r6`, and `tmp7lj5knwe`. They are ignored only when `git status --short` shows no unrelated project files.

## Safety

- No live internet/API calls made during Codex validation.
- No live LLM/API calls made.
- No push performed.
- No PR created.
- No merge performed.
- No tag created.
- No release created.
