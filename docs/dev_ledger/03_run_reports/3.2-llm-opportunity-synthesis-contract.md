# 3.2 LLM Opportunity Synthesis Contract Run Report

## Summary

- Branch: `feat/v2-5-block-3-opportunity-formation`
- Roadmap item: `3.2 LLM opportunity synthesis contract`
- Result: implemented and locally validated
- Live collection: not run
- Live internet/API calls: none
- Live LLM/API calls: none
- Provider execution: none

## Files

- `src/oos/llm_opportunity_synthesis_contract.py`
- `src/oos/llm_contracts.py`
- `tests/test_llm_opportunity_synthesis_contract.py`
- `docs/roadmaps/OOS_roadmap_v2_5_opportunity_formation_and_founder_learning_checklist.md`
- `docs/dev_ledger/00_project_state.md`
- `docs/dev_ledger/02_mini_epics/3.2-llm-opportunity-synthesis-contract.md`
- `docs/dev_ledger/03_run_reports/3.2-llm-opportunity-synthesis-contract.md`

## Implementation Notes

- Added `LLMOpportunitySynthesisInput` and `LLMOpportunitySynthesisContract`.
- Added `build_opportunity_synthesis_prompt`, `build_opportunity_synthesis_messages`, and `build_opportunity_synthesis_request`.
- Added response validation for required schema fields, evidence IDs, cited evidence, confidence bounds, and `advisory_only=true`.
- Added `ensure_evidence_bound_response` to reject citations, signal IDs, and source URLs that are not present in the supplied `EvidencePack`.
- Added high-risk evidence guardrails so weak/generic/vendor/duplicate/insufficient contexts cannot be returned with high confidence.
- Added local-preview budget role `opportunity_synthesis` while leaving disabled/default provider behavior unchanged.

## Validation

- `$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest tests.test_llm_opportunity_synthesis_contract -v`
- `$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest tests.test_opportunity_sketch_baseline -v`
- `$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest tests.test_evidence_pack_builder -v`
- `$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\oos-validate.ps1`
- `git diff --check`
- `git status --short --untracked-files=no`

## Roadmap State

- Current item: `3.3 Offline LLM opportunity synthesis dry-run`
- Completed: `11 / 24`
- Remaining: `13 / 24`

## Explicit Safety Notes

- No live collection run was performed.
- No live internet/API calls were made.
- No live LLM/API calls were made.
- `provider.complete()` was not called.
- Unit tests used fixture-only local data.
