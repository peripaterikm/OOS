# 2.1 RawEvidence Model and Artifact Store Run Report

## Summary

- Roadmap item: `2.1` - RawEvidence model and artifact store
- Branch: `feat/2-1-raw-evidence-model-artifact-store`
- Working directory: `C:\MARK\My_projects\OOS`
- Commit state during validation: pre-commit local changes
- Result: pass

## Commands Run

- `git branch --show-current`
  - Result: pass - `feat/2-1-raw-evidence-model-artifact-store`
- `git status --short`
  - Result: pass - only known `.test-tmp` / `.tmp_tests` ACL warnings before implementation
- `git log --oneline -5`
  - Result: pass - recent local history inspected
- `$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -m unittest tests.test_raw_evidence_model -v`
  - Result: pass - ran 8 tests, OK
- `$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -m unittest tests.test_artifact_store_roundtrip -v`
  - Result: pass - ran 1 test, OK
- `.\scripts\oos-validation-report.ps1 -ReportPath docs\dev_ledger\03_run_reports\2.1-raw-evidence-model-artifact-store.md -FocusedTest tests.test_raw_evidence_model`
  - Result: partial helper issue - focused tests and full unittest discovery passed, then helper returned nonzero while entering `.\scripts\oos-validate.ps1` because PowerShell surfaced verbose unittest stderr as `NativeCommandError`; remaining validation commands were rerun manually.
- `$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v`
  - Result: pass - ran 283 tests, OK
- `.\scripts\oos-validate.ps1`
  - Result: pass - OOS validation complete
- `.\scripts\verify.ps1`
  - Result: pass - Verification complete
- `git diff --check`
  - Result: pass - no whitespace errors

## Known Warnings

- Initial sandboxed focused tests hit the documented Windows `TemporaryDirectory` ACL issue under `%TEMP%`; the same focused tests passed outside the sandbox.
- `git status --short` reports known ACL warnings for stale `.test-tmp` / `.tmp_tests` directories. No real unrelated project files were modified before implementation.
- The validation helper created an intermediate `.log` file while capturing output; the final committed evidence is this Markdown report.

## Scope Confirmation

- RawEvidence model and artifact store support implemented.
- No source registry implemented.
- No query planner implemented.
- No collectors implemented.
- No network/API calls made.
- No live LLM calls made.
- No secrets touched.
- No push performed.
- No merge performed.
- No tag created.
- No release created.
