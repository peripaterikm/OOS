# Run Report 1.1 - Weekly Run Artifact Contract

## Branch

`feat/v2-6-weekly-loop-block-1`

## Summary

Implemented the canonical `WeeklyRunManifest` model and read/write helpers that define the exact set of 14 artifacts every v2.6 weekly cycle run must produce. This is the foundational contract — every downstream consumer (unified cycle builder, founder inbox v2, status command, run reports, decision import) reads from these well-known paths under `artifacts/weekly_runs/{run_id}/`.

## Files

- `src/oos/weekly_run_manifest.py` — `WeeklyRunManifest` model, read/write helpers, run_id generation, canonical path/schema helpers
- `tests/test_weekly_run_manifest.py` — 39 focused tests
- `docs/dev_ledger/02_mini_epics/1.1-weekly-run-artifact-contract.md`
- `docs/dev_ledger/03_run_reports/1.1-weekly-run-artifact-contract.md`
- `docs/roadmaps/OOS_roadmap_v2_6_real_weekly_loop_operationalization_checklist.md`
- `docs/dev_ledger/00_project_state.md`

## Validation Commands

- `powershell -NoProfile -Command "$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest tests.test_weekly_run_manifest -v"`
- `powershell -NoProfile -Command "$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest discover -s tests -p 'test_*.py' -v"`
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\oos-validate.ps1`
- `git diff --check`
- `git status --short --untracked-files=no`

## Acceptance Result

- `WeeklyRunManifest` model with `run_id`, `created_at`, `schema_version`, `artifact_paths` (14 entries), `artifact_schema_versions` (14 entries), `empty_states` (14 entries), `input_file`, `input_signal_count`, `advisory_only`, `no_live_api`, `no_live_llm`: complete.
- `write_weekly_run_manifest()` writes and validates: complete.
- `read_weekly_run_manifest()` reads, validates, and path-traversal-guards: complete.
- Validation rejects missing fields, unknown schema versions, `..` traversal, absolute paths, missing manifest entry: complete.
- Empty-state manifest with all `empty_states=true`: complete.
- Full 14-artifact-path fixture roundtrip: complete.
- Deterministic `run_id = weekly_run_{YYYY-MM-DD}_{content_hash_short}`: complete.
- JSON roundtrip stable with deterministic key ordering: complete.
- Focused tests: 39/39 OK.

## Safety Notes

- No live collection was run.
- No live internet/API calls were made.
- No live LLM/API calls were made.
- `provider.complete()` was not called.
- Unit tests use temporary directories only; no real `artifacts/` written.
- `advisory_only=True`, `no_live_api=True`, `no_live_llm=True` enforced by validation.

## Roadmap State

- Current item after this run: `2.1` — Unified weekly cycle builder
- Completed: `1 / 9`
- Remaining: `8 / 9`
