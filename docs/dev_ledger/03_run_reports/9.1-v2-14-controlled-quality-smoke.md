# v2.14 Controlled Quality Smoke — Run Report (v2.14-FINAL)

**Roadmap Item:** v2.14 Item 9 — Controlled pilot smoke re-run with v2.14 quality gates (COMPLETE)
**Date:** 2026-05-16
**Run ID:** `v2_14_quality_smoke_deterministic`
**Status:** Complete — controlled quality smoke passes after follow-up fixes (59 PASS / 0 FAIL)

---

## Codex Review Findings Addressed

This run report documents the Codex review fixes for Item 9. The original smoke passed vacuously on several gates. The following issues are now fixed:

1. **SQR false-clean gates hardened (A1–A9):** Gates now read SQR JSON directly and assert `noise_signal_total > 0`, `weak_signal_total > 0`, `classification_health != "clean"`, `evidence_quality_status != "clean"`, `flagged_record_count > 0`, `len(contradiction_warnings) > 0`, at least one per-source warning, and non-empty Markdown warning bullets.
2. **Opportunity synthesis gap fixed (D1):** Gate D1 now requires `len(opp_candidates) >= 1` instead of just `isinstance(list)`. Fixture includes unknown-actor evidence pair to deterministically produce at least one candidate.
3. **B2/B2b cluster gate hardened:** B2 requires `len(trace_cluster_ids) == 1` (exactly one cluster) for the stack/trace pair (`v214_gh_stack_001` + `v214_gh_trace_001`). B2b verifies `v214_gh_prov_001` is in a SEPARATE cluster, not merged with the trace-debugging items.
4. **A5/A9 Markdown warning gate hardened:** Checks for non-empty bullet content, not just section headers.
5. **Roadmap/runbook/report synchronized:** All now state the hardened requirements.

---

## Command Run

```powershell
.\scripts\run-controlled-smoke.ps1
```

Step 11 (v2.14 Controlled Quality Smoke) executed within the controlled smoke script. The step runs in a temporary root directory. No live APIs. No LLMs. No network.

---

## Fixture Summary (v2.14-FIX)

| Evidence ID | Source | Kind | Quality Flags | Expected |
|-------------|--------|------|---------------|----------|
| `v214_gh_stack_001` | GitHub Issues | bug_report | debugging_pain | Accepted |
| `v214_gh_trace_001` | GitHub Issues | bug_report | debugging_pain, workaround_signal | Accepted |
| `v214_gh_prov_001` | GitHub Issues | bug_report | debugging_pain | Accepted |
| `v214_hn_noise_001` | HN | product_launch | requires_manual_review, suspected_self_promo, low_confidence_source, vendor_promo | Noise |
| `v214_hn_noise_002` | HN | product_launch | requires_manual_review, low_confidence_source, launch_hype | Noise |
| `v214_hn_flagged_001` | HN | pain_signal_candidate | low_text_context, requires_manual_review, low_confidence_source | Weak/Noise |
| `v214_hn_pain_001` | HN | pain_signal_candidate | debugging_pain, workaround_signal, cost_signal | Accepted (flagged) |
| `v214_hn_clean_001` | HN | pain_signal_candidate | (none) | Accepted |
| `v214_hn_unknown_001` | HN | pain_signal_candidate | debugging_pain, workaround_signal | Accepted |
| `v214_gh_unknown_001` | GitHub Issues | bug_report | debugging_pain | Accepted |

**v2.14-FIX changes:**
- Added `v214_hn_unknown_001` and `v214_gh_unknown_001` — unknown-actor pain evidence pair with `topic_id = "unknown_actor_debugging"` and `target_user = "unknown"`.
- Changed `v214_gh_prov_001` `topic_id` from `"agent_provenance"` to `"agent_debugging_traces"`. The stack/trace pair (`v214_gh_stack_001` + `v214_gh_trace_001`) coheres in one cluster; provenance (`v214_gh_prov_001`) remains in a separate cluster by design. The shared topic_id enables eligibility through the cluster assembly pipeline without forcing all three into one cluster.

All source_urls are valid `http(s)://`. 6 HN + 5 GitHub Issues.

---

## Gates Checked (HARDENED v2.14-FIX)

### Gate A — Source Quality Report

| # | Assertion | Previous | Hardened |
|---|-----------|----------|----------|
| A1 | noise_signal_total > 0 | weak (optional) | mandatory from SQR JSON |
| A2 | weak_signal_total > 0 | weak (optional) | mandatory from SQR JSON |
| A3 | classification_health != "clean" | conditional | mandatory when fixture has noise/weak |
| A4 | evidence_quality_status != "clean" | allowed "clean" | requires != clean |
| A5 | flagged_record_count > 0 | missing | NEW: mandatory |
| A6 | dominant_quality_flags include evidence-only flags | existing | preserved |
| A7 | contradiction_warnings count > 0 | isinstance check | len(cw) > 0 |
| A8 | at least one per-source warning | missing | NEW: mandatory |
| A9 | Markdown warning bullets not empty | headers only | non-empty bullets |

### Gate B — PainCluster Assembly

| # | Assertion | Previous | Hardened |
|---|-----------|----------|----------|
| B1 | Multiple clusters (> 1) | existing | preserved |
| B2 | Coherent stack/trace items (`v214_gh_stack_001` + `v214_gh_trace_001`) in exactly 1 cluster | ≤ 2 clusters | == 1 cluster |
| B2b | Provenance item (`v214_gh_prov_001`) in separate cluster | implicit | explicit: prov_cluster != trace_cluster |
| B3 | No dead/nme titles | existing | preserved |
| B4 | Zero catch-all risk | existing | preserved |

### Gate C — Founder Review Package

Unchanged.

### Gate D — Opportunity Synthesis

| # | Assertion | Previous | Hardened |
|---|-----------|----------|----------|
| D1 | Opportunity candidate count | may be empty | >= 1 |
| D2 | not_a_solution_yet | existing | hardened: fails if empty |
| D3 | created_by deterministic_stub | existing | hardened: fails if empty |
| D4 | evidence_links preserved | existing | hardened: fails if empty |
| D5 | Synthesized hypothesis required fields | missing | NEW: not_a_solution_yet + created_by + evidence_links |
| D6 | Known-actor ICP not unproven | existing | hardened: ensures known actors don't get unproven ICP |

*D6 checks known-actor hypotheses do not carry `target_icp='unproven; validate actor'`. Unknown-actor synthesis behavior is covered by `test_opportunity_synthesis.py` regression tests.*

---

## Eligibility Path for Opportunity Candidates

The v2.14-FIX fixture creates an eligibility path for deterministic opportunity synthesis:

1. **Clean GitHub stack/trace/debug cluster:** `v214_gh_stack_001` and `v214_gh_trace_001` share `topic_id = "agent_debugging_traces"` with concrete debugging pain, clean source_urls, and no promotion blockers. This cluster receives PROMOTE or NEEDS_MORE_EVIDENCE recommendation and passes `_cluster_is_eligible()`. The provenance item (`v214_gh_prov_001`) shares the same `topic_id` but is placed in a SEPARATE cluster by the assembly engine; it is eligible independently. Either eligible cluster produces opportunity candidates via `synthesize_opportunities()`.
2. **Unknown-actor evidence:** `v214_hn_unknown_001` and `v214_gh_unknown_001` share `topic_id = "unknown_actor_debugging"` with `target_user = "unknown"`. Under current cluster assembly policy (unknown actor does NOT block merge when workflow+object match), these items may merge into a known-actor cluster or form their own. The `target_actor='unknown'` → `target_icp='unproven; validate actor'` path is covered by `test_opportunity_synthesis.py` regression tests; unknown-actor behavior is **regression-tested, not smoke-tested**.

At least one eligible cluster must produce an opportunity hypothesis via `synthesize_opportunities()`.

---

## Pass/Fail Table (Validated)

| Gate | Passes | Fails |
|------|--------|-------|
| A — Source Quality Report | 9 | 0 |
| B — PainCluster Assembly | 5 | 0 |
| C — Founder Review Package | 5 | 0 |
| D — Opportunity Synthesis | 6 | 0 |
| Script-level steps | 34 | 0 |
| **TOTAL** | **59** | **0** |

---

## SQR Contradiction Checks (Hardened)

- `noise_signal_total > 0` verified from SQR JSON (not Markdown).
- `weak_signal_total > 0` verified from SQR JSON.
- `classification_health` is not `"clean"` when noise/weak evidence exists.
- `evidence_quality_status` is not `"clean"`.
- `flagged_record_count > 0`.
- `contradiction_warnings` list has entries.
- At least one per-source warning exists.
- Markdown contains non-empty warning bullets under warning sections.

---

## Cluster/Title Checks (Hardened)

- Mixed anchors produce multiple coherent clusters, not one catch-all.
- Coherent stack/trace pair (`v214_gh_stack_001` + `v214_gh_trace_001`) share exactly one cluster (B2).
- Provenance item (`v214_gh_prov_001`) is in a separate cluster (B2b).
- No cluster has `catch_all_risk = true`.
- No generated title contains `[dead]` or `needs_more_evidence`.

---

## Opportunity Synthesis Checks (Hardened)

- At least 1 opportunity candidate synthesized (not zero).
- All hypotheses carry `not_a_solution_yet = true` and `created_by = deterministic_stub`.
- `evidence_links` are non-empty on all hypotheses (D4).
- D5: Synthesized hypothesis has required fields (not_a_solution_yet + created_by + evidence_links).
- D6: Known-actor hypotheses do NOT carry `target_icp = "unproven; validate actor"`.
- Unknown-actor → `unproven; validate actor` behavior is covered by `test_opportunity_synthesis.py` regression tests.

---

## Regression Fixture Status

- `tests/test_v2_14_pilot_regression_fixtures.py` — existing tests continue to pass.
- `tests/test_controlled_weekly_run_smoke.py` — updated with hardened gate tests and run-report content guards.

---

## Artifact Policy Confirmation

- All smoke output written to `$TempRoot/v2_14_quality_smoke/` (temp directory).
- No artifacts written to repository `artifacts/`.
- No runtime artifacts committed.
- No live APIs, no LLMs, no source expansion.

---

## Final Validation State (v2.14-FIX)

| Validation Step | Result |
|-----------------|--------|
| `run-controlled-smoke.ps1` | 59 PASS / 0 FAIL |
| `dev-test.ps1 -Full` | 2817 OK |
| `dev-git-check.ps1` | 6/6 PASS |
| `git status --short` | clean / working tree clean |
| `git diff --check` | clean |

All cross-validation gates pass. No stale comments remain in the smoke script. Test guards confirm report/script consistency.

---

## Final Readiness Statement for Item 10

Item 10 (Final v2.14 Checkpoint) is next. Item 9 Codex review fixes (roadmap state, D6 alignment, runbook/report honesty, B2/B2b documentation, eligibility path wording, final validation state, stale comment removal, test guards for report/script consistency) are complete in this commit.

---

*v2.14 Item 9 — Controlled Quality Smoke (COMPLETE). Run report. Does not modify source code, tests, scripts, or pipeline behavior beyond the allowed files.*
