# Run Report: 6.1 — Weekly Cycle Status Command v2

- **Item**: 6.1
- **Date**: 2026-05-07
- **Branch**: `feat/v2-6-weekly-loop-block-1`
- **Status**: Done

## Build Output

- `src/oos/weekly_cycle_status.py` created (652 lines)
- `src/oos/cli.py` modified (+28 lines)
- `tests/test_weekly_cycle_status_v2.py` created (939 lines)

## Validation

### Focused Tests

```
$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest tests.test_weekly_cycle_status_v2 -v
```

```
Ran 29 tests in 1.908s
OK
```

### Test Classes

| Class | Tests | Description |
|-------|-------|-------------|
| `WeeklyCycleStatusModelTests` | 3 | Model serialization, JSON/Markdown rendering |
| `WeeklyCycleStatusMockRunTests` | 10 | Mock run reading: valid, missing manifest, missing artifacts, corrupt JSON, auto-discover, empty runs |
| `WeeklyCycleStatusIntegrationTests` | 5 | Real weekly cycle builder integration, read-only enforcement, empty input, corrupt manifest, invalid run_id |
| `WeeklyCycleStatusCLITests` | 11 | CLI existence, output, exit codes, JSON mode, auto-discover, read-only, existing commands, safety flags |

### Key Assertions Verified

- Status reports all artifacts present after normal weekly run
- Status reports founder inbox present and review item count
- Status reports no decisions before import; recommends review
- Status reports decisions imported after import with correct counts
- Status handles missing manifest → manifest_valid=False, validation_passed=False
- Status handles missing artifact → in missing_artifact_keys, validation_passed=False
- Status handles invalid JSON → error recorded, no crash
- Status auto-discovers latest run by directory name
- Status fails clearly when no runs exist
- CLI exit codes: 0/1/2 correct
- CLI is read-only (no artifact modification)
- Existing `run-weekly-cycle-v2` still works
- Existing `import-founder-decisions-v2` still works
- Advisory-only, no_live_api, no_live_llm all True

## Implementation Notes

- Missing artifacts are reported as **errors** (not warnings) so they flow into `validation_passed=False` and exit code 1.
- JSON list artifacts: count list length via `items` key or direct list length.
- JSON dict artifacts with no clear collection: count as 0 (present but non-list).
- Markdown artifacts: counted as present/non-present only.
- Invalid JSON does not crash; error recorded, validation_passed=False.
- Manifest always validated via `read_weekly_run_manifest()`.
- Latest run discovery: directories sorted by name descending (ISO date prefix ensures chronological order).

## Risk Assessment

- Low risk: pure read-only addition, no mutation of any existing artifact.
- Conservative: uses existing `canonical_artifact_paths()` and `read_weekly_run_manifest()` from `weekly_run_manifest.py`.
- No new dependencies, no new API calls.
- All tests use temp directories.
