# Run Report — Roadmap v2.4 Item 3.1 Scoring Model v2

## Scope

- Branch: `feat/3-1-scoring-model-v2`
- Roadmap item: `3.1` — Scoring model v2 and relevance dimensions
- Commit target: `Add signal scoring model v2`

## Changes Made

- Added `src/oos/signal_scoring.py` with explicit deterministic scoring inputs and breakdowns.
- Integrated scoring v2 into `src/oos/candidate_signal_extractor.py`.
- Extended `CandidateSignal` with optional `scoring_model_version` and `scoring_breakdown` fields.
- Added focused scoring tests in `tests/test_signal_scoring_model_v2.py`.
- Updated candidate-signal extractor tests for `rule_based_v2` artifact metadata.
- Updated the v2.2 completion-checkpoint status regex to allow Roadmap v2.4 `5 / 17` state.
- Updated `.gitignore` to ignore generated Codex/temp validation directories.
- Updated Roadmap v2.4 and Dev Ledger state.

## Scoring Model v2

- Current mode: `signal_scoring_v2_embeddings_disabled`.
- Embeddings, semantic relevance, and LLM review are not implemented in this item.
- Component weights sum to 1.0 in disabled mode.
- `signal_type_multiplier` is a soft multiplier, not a hard ranking priority.
- Caps and penalties are explicit:
  - low topic relevance cap
  - anti-marketing cap
  - needs-human-review cap
  - noise cap

## Backward Compatibility Notes

- `CandidateSignal.confidence` remains the ranking field used by downstream founder package behavior.
- Existing `CandidateSignal` artifacts can still load because the new scoring fields have defaults.
- New artifacts include scoring metadata for inspectability.

## Validation Evidence

Commands run during implementation:

```powershell
$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -m unittest tests.test_signal_scoring_model_v2 -v
$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -m unittest tests.test_candidate_signal_extractor tests.test_evidence_cleaner_classifier tests.test_founder_discovery_package_lite tests.test_live_quality_smoke tests.test_customer_voice_query_planner_integration -v
```

Results:

- Focused scoring tests: `OK` — 16 tests.
- Relevant regressions: `OK` — 92 tests.

Additional validation to record before commit:

```powershell
$env:PYTHONPATH="src"; .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v
.\scripts\oos-validate.ps1
.\scripts\verify.ps1
git diff --check
```

Final validation results:

- Full unittest discovery: `OK` — 552 tests.
- `.\scripts\oos-validate.ps1`: initial run blocked by PowerShell execution policy / known Temp ACL behavior; rerun with `powershell -ExecutionPolicy Bypass -File .\scripts\oos-validate.ps1` outside the sandbox passed.
- `.\scripts\verify.ps1`: initial run blocked by PowerShell execution policy / known Temp ACL behavior; rerun with `powershell -ExecutionPolicy Bypass -File .\scripts\verify.ps1` outside the sandbox passed.
- `git diff --check`: passed with line-ending warnings only.

Known local ACL note:

- Non-escalated full discovery and validation script runs can fail on Windows temp cleanup with `PermissionError: [WinError 5] Access denied` under temp directories. Escalated validation runs passed without changing code or making live network calls.

## Known Limitations

- Semantic relevance provider is deferred to Roadmap v2.4 item `3.2`.
- LLM signal review is deferred to later Roadmap v2.4 items.
- No embeddings are used.

## Safety Notes

- No live internet/API calls were made during validation.
- No live LLM/API calls were made.
- No dependencies were added.
- No secrets were touched.
- No push, PR, merge, tag, or release was performed.
