# 1.5 Mojibake Cleanup Regression Run Report

## Branch

- `feat/v2-5-block-1-first-run-quality-hardening`

## Scope

Implemented Roadmap v2.5 item `1.5` by extending deterministic mojibake cleanup and adding regression coverage for founder-facing outputs.

## Inputs

- Founder labels: `examples/first_real_open_source_signal_run_v1/founder_manual_labels.json`
- Roadmap: `docs/roadmaps/OOS_roadmap_v2_5_opportunity_formation_and_founder_learning_checklist.md`

## Outputs

- `src/oos/evidence_classifier.py`
- `tests/test_mojibake_cleanup.py`
- `tests/test_evidence_cleaner_classifier.py`

## Implementation Summary

- Added deterministic repairs for common UTF-8-as-Windows-1252 mojibake, including apostrophes, quotes, bullets, dashes, ellipses, and NBSP-style fragments.
- Preserved valid Unicode punctuation such as curly quotes, em dashes, and bullets.
- Ensured cleaned evidence, candidate summaries, and founder package Markdown/JSON avoid founder-facing mojibake fragments.
- Preserved raw evidence, `evidence_id`, and `source_url` traceability.

## Validation Evidence

- `$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest tests.test_mojibake_cleanup -v`
  - Result: passed, `Ran 8 tests ... OK`.
- `$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest tests.test_evidence_cleaner_classifier -v`
  - Result: passed, `Ran 27 tests ... OK`.
- `$env:PYTHONPATH='src'; .\.venv\Scripts\python.exe -m unittest discover -s tests -p "test_*.py" -v`
  - Result: passed outside sandbox after Windows temp-directory permissions blocked the sandbox run, `Ran 726 tests ... OK`.
- `powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\oos-validate.ps1`
  - Result: passed outside sandbox after the same Windows temp-directory permissions blocked the sandbox run.
- `git diff --check`
  - Result: passed.

## Roadmap State

- Current item after completion: `1.6 Second controlled live run comparison`
- Completed: `5 / 24`
- Remaining: `19 / 24`

## Safety Confirmations

- Push performed: `no`
- PR created: `no`
- Merge performed: `no`
- Tag/release created: `no`
- Additional live collection run: `no`
- Live internet/API calls: `0`
- Live LLM/API calls: `0`
- Dependencies added: `0`
